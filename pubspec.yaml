name: agendequadra
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Dependência para persistência de dados
  shared_preferences: ^2.0.11  # Adicionada para salvar dados localmente

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  # Adicionando a seção de assets para incluir imagens no projeto
  assets:
    - assets/sports_banner.png
